# GLB / 可视化（扁平：`assets/glb/` 根目录即交付物）

本目录与 Aloha / RM65 / Bobacbot 保持一致：交付相关文件直接放在 **`assets/glb/`** 下（与本 `README.md` 同级）。

## 当前文件（资产流水线生成）

| 文件 / 目录 | 说明 |
|-------------|------|
| `H1_PRO.glb` | 由 `../mjcf/H1_PRO.xml` 导出并压缩 |
| `H1_PRO.xml` | **归一化后的 MJCF**（与 `../mjcf/` 中仿真用 XML **不是同一份**） |
| `assets/` | 流水线写出的网格，供上述 `H1_PRO.xml` 引用 |

## 上游 GLB（追溯）

上游资产包自带 `Zerith_H1_PRO.glb`，已保留在本目录中用于对照与追溯。当前交付以 pipeline 产物 `H1_PRO.glb` 为准。

## 重新生成

本目录为交付产物；需要重新生成时使用团队资产流水线（仓库外工具）。需要 **1080 预览图** 时需安装 Blender。
